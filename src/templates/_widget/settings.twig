{% import '_includes/forms.twig' as forms %}

{{ forms.selectField({
    label: 'Shelf'|t('mybooks'),
    id: 'shelf',
    name: 'shelf',
    options: shelfOptions,
    value: widget.shelf,
}) }}

{{ forms.textField({
    label: 'Books per person'|t('mybooks'),
    id: 'limit',
    name: 'limit',
    type: 'number',
    min: 1,
    max: 24,
    size: 3,
    value: widget.limit,
    errors: widget.getErrors('limit'),
}) }}
